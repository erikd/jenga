resolver: lts-9.0

extra-deps:
 # Override the defaul version of directory provided by this resolver.
 - directory-1.3.1.0
 # Add wide-word which (probably) isn't available in this resolver.
 - wide-word-0.1.0.5

packages:
 - '.'
